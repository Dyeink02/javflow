# JavFlow 审计报告（v2 · 2026-09-12 深度复核版）

> 基线报告：`PROJECT_AUDIT_REPORT.md`（2026-07-28）
> v1 复核：2026-09-12 上午（清单式对照）
> **v2 深度复核：2026-09-12（以运行时可达性为准绳重新判定，推翻 v1 两处结论，补全一处修复）**
> 结论速览：**约 2.7 万行"债务"其实是断线死代码（可直接删除），而非需要重构的重复活代码；活代码内真正的问题比 v1 报告更少、也更具体。**

---

## 一、v2 复核方法论（为什么结论会变）

v1 复核用的是"存在性检查"——目录/文件在不在、函数重不重复。这次升级为**运行时可达性判定**，三条铁证链：

1. **前端 bundle 清单**（`scripts/frontend-bundle-files.js`）→ 只有 `common/`、`renderer/` 共 60+ 文件进入运行时；构建产物 `wails-shell/frontend/desktop/renderer/bundle.js`（818KB）逐串验证。
2. **Go 命令分发链**（`internal/bridge/api_dispatch_chain.go`）→ 只有 `subscription-v2` 域在链上。
3. **go:embed 嵌入树**（`wails-shell/frontend/` 仅含 desktop 产物，无 dist/src）。

**教训**：文件树形态 ≠ 活代码。空目录、无引用文件在"存在性检查"里都是"问题"，在可达性检查里有的是"已解决"，有的是"该删除"。

---

## 二、对 v1 对照表的三处修正

| v1 判定 | v2 修正判定 | 铁证 |
|---------|------------|------|
| 2.2 订阅 V1/V2 并存「未动」 | ✅ **已解决** | `internal/avsubscription/`、`internal/subcrawl/` 是**空目录**（V1 代码早已删除），分发链只有 `subscription-v2`。收尾动作：删掉空目录即可 |
| 2.4+2.6 scraperRunner 24 文件、TS/Go 双引擎「未动，建议合并」 | ✅ **性质改判：整体是死代码，该删不该合** | `src/` 15,986 行 TS：0 个 bundle 清单项、desktop 0 处引用、0 个测试引用；package.json 无 main 入口；tsc 产物 dist/ 不进 exe。**唯一活引擎是 Go** |
| 2.7 sidecar 归档层「未动」 | ⚠️ 性质改判：**不可达死代码** | `desktop/sidecar` + `desktop/mainServices` 共 8,969 行，bundle 清单 0 项（原 `JAV_ENABLE_ARCHIVED_SIDECAR_DOMAINS` 门控早已无意义） |

v1 其余判定经复核维持不变（2.1 备份目录、2.3 工具函数重复、2.5 bridge 72 文件、4.1/4.2/4.4、依赖错位、4.3 误报）。

## 三、v2 新发现

### 3.1 [高] 死代码全景（可删除 ~1.1 万行；修正后）

| 集群 | 行数 | 死因（铁证） |
|------|------|-------------|
| `desktop/sidecar` + `desktop/mainServices` | 8,969 | 不进渲染端 bundle（**修正（2026-09-13）**：`desktop/sidecar/index.js` 是 Node sidecar 入口，经 dist/core 在 CF 绕过通道运行时执行——sidecar 本体与 dist 不可删；`src/`（15,986 行）是 dist 的源码，同理为 sidecar 通道活代码，Go 原生路径不经过它） |
| `desktop/common/actressRanking*.js`（5 文件） | 1,613 | **构建产物 bundle.js 中 0 命中**（运行时实锤）；仅被同样缺席清单的兄弟文件和死掉的 sidecar 引用 |
| `internal/avsubscription/`、`internal/subcrawl/` | 0（空目录） | 代码已删，目录残留 |

基线报告"30-40% 冗余"的方向判断正确，但**性质判断错误**：冗余主体不是"并存的重复实现"（那需要高风险合并重构），而是**断线残留**（低风险删除）。最优解从"合并为 6-8 个文件"变为"整目录删除 + 依赖联动清理"。**修订（2026-09-13）**：`magnet2torrent-js` 是严格校验的运行依赖（sidecar 通道实锤），不可移除；commander/cli-progress 仍待核实。

### 3.2 [流程风险] 本次开发曾在死文件上做功

git 工作区显示 `desktop/common/actressRankingService.js` 在 0.4.4 开发周期内被修改过——而它是死代码。连本项目最熟悉代码的维护者都会误判文件活性，说明项目缺一道"活性门禁"。**建议**：`verify-maintainability.js` 已引用 bundle 清单做边界检查，顺理成章加一条规则——**清单之外、desktop/ 之下、被 git 追踪且有近期改动的文件 → 告警**。

### 3.3 [已修复] v1 数据竞争修复不完整，本次补全

v1 把 `filmsQueued`/`filmsAttempted` 改为 atomic，但漏了同一并发路径上的 `filmCount`——`EventFilmDataSaved` 回调（worker goroutine）执行 `r.filmCount = r.writer.RecordCount()`，与统计/保存路径并发。本次补全（`runner.go`、`diagnostics.go`、`diagnostics_test.go`），并复核确认 `recordDetailFailure`/`emitLog` 均有锁、无其他裸共享计数器。`go build` + `go vet` + 包测试全绿。

---

## 四、活代码内的真实债务（v2 修正后的最终清单）

| 优先级 | 事项 | 性质 |
|--------|------|------|
| 立即 | 提交已暂存的 `wails-shell/internal/bridge/data/`（276 文件，8.6MB） | 不提交则换机无法编译（go:embed 依赖） |
| 立即 | 提交 crawlrunner 两个数据竞争修复 | 已验证，待 commit |
| 短期 | 删除死代码集群（上表 2.7 万行）+ 联动 npm 依赖 + 空目录 | 纯删除，收益最大风险最小 |
| 短期 | 门禁加死代码活性检查 | 防止再次在死文件上做功 |
| 中期 | formController 监听器解绑（基线 4.1）；`@types/*` 移出 dependencies | |
| 规划 | bridge 72 文件、224 个占位注释、`lib/pq` | 活代码内的整洁度问题，按版本节奏做 |
| 择机 | 清理父目录 21 个备份目录 | git 侧已干净（.git 仅 13MB） |

**测试健康度（两轮实测一致）**：`go build`/`go vet`/`go test ./...` 全绿；mocha 221 用例全绿且 100% 测活代码（0 个测试引用 src/）；Go 测试 126 个文件。基线称赞的安全实践全部在位。

---

## 五、高维度总结判断

1. **这个项目的真实健康度比 v1 报告呈现的更好**：三个 P0 级架构债里两个（V1 并存、双引擎）实际上已经解决——只是没删干净尸体，让"存在性检查"产生了"未动"的假象。
2. **债务形态决定了最优策略**：当前债的主体是"不可达代码"，处理手段是**删除**（半天工作量、零行为风险），而不是基线报告规划的"合并重构"（按版本计的高风险工程）。v1 报告如果被当作施工图执行，会把精力浪费在给死代码做合并。
3. **治理骨架已有、差一块**：bundle manifest + verify-maintainability 边界门禁 + 审计标记体系（H-07/H-14/H-15…）说明项目有自我约束能力；缺的只是把"活性"也纳入门禁。加上那一条规则后，死代码将无法再悄悄回潮。
4. **对维护者（含 AI）的启示**：在这个仓库里判断任何文件能不能改，先查三处——bundle 清单、分发链、embed 树；三者都查不到的引用关系一律视为不可达。

---

## 附录：MetaTube SDK（v1.4.0）数据源与榜单借鉴（维持 v1 结论，摘要）

- 刮削引擎经 `engine.Default()` 全量注册 **38 个 provider**（官方大厂 fanza/mgstage/sod/faleno/dahlia；聚合站 javbus/jav321/duga/avbase 等；素人系 1pondo/caribbean/heyzo 等；FC2 系；特殊资产 **gfriends** 社区头像库）。
- **榜单借鉴优先级**：① FANZA 周榜（与现有月榜同解析器，近零成本；SDK 源码实证 DMM GraphQL 响应自带 `weeklyRanking`/`monthlyRanking` 字段）→ ② FANZA 通贩年榜（与现有租赁年榜交叉）→ ③ MGStage、DUGA 周/月榜（无 CF 墙，接入前需在线验证）→ ④ **gfriends 头像库**（数万张按名索引，系统性解决图鉴头像覆盖率，当前仅内嵌 276 张）。
- 反向结论：javdb/javlibrary 等强 CF 站不在 provider 内也不建议做榜单源；榜单数据要求离线即开，官方站契合现有架构。
