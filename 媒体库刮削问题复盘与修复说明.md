# 媒体库刮削与视频整理问题复盘

更新时间：2026-07-29

## 结论

本次问题不是 Javbus 页面没有记录，也不是佐山愛演员过滤把这三部影片过滤掉，而是同一条爬虫记录的“规范番号”和磁力文件名的 `dn` 显示名不一致。旧的视频整理器只把文件名解析出的 `MXGS-1121`、`MXGS-1183`、`MXGS-1358` 与规范名单逐字比较，因此严格模式把它们放进了“未命中”。

现在整理器会读取已加载爬虫快照中的磁力链接 `dn`，建立“磁力别名 -> 规范番号”的内存索引：

| 本地文件中的显示名 | 爬虫记录中的规范番号 | 证据 |
| --- | --- | --- |
| `MXGS-1121` | `MXGS-112` | `filmData.json` 中 `MXGS-112` 的磁力 `dn=MXGS1121` |
| `MXGS-1183` | `MXGS-118` | `filmData.json` 中 `MXGS-118` 的磁力 `dn=MXGS-1183` |
| `MXGS-1358` | `MXGS-135` | `filmData.json` 中 `MXGS-135` 的磁力 `dn=MXGS1358` |

匹配成功后，整理器内部使用规范番号进行归类和改名；不会把磁力别名扩展成新的爬虫番号。原始文件路径仍保留在日志和报告中，便于追溯。

## 现场证据

用户提供的 `F:\里番\未识别番号视频.txt` 中记录了：

```text
MXGS-1121 | Z:\佐山愛\MXGS1121\mxgs01121.mp4
MXGS-1183 | Z:\佐山愛\MXGS-1183\hhd800.com@MXGS-1183.mp4
MXGS-1358 | Z:\佐山愛\kfa55.com@MXGS1358\kfa55.com@MXGS1358.mp4
```

对 `C:\Users\Administrator\Desktop\磁力链接\佐山愛\filmData.json` 的只读检查结果：

- 记录总数：428 条。
- `MXGS-112` 记录的磁力显示名为 `MXGS1121`。
- `MXGS-118` 记录的磁力显示名为 `MXGS-1183`。
- `MXGS-135` 记录的磁力显示名为 `MXGS1358`。
- 按同一番号前缀和连续数字关系筛选后，可建立 8 条唯一别名；现场快照没有冲突别名。冲突场景由回归测试覆盖并会拒绝猜测。

媒体库目录 `Y:\docker\emby-nginx\strm\av\限制级\女优\女优影视库\佐山愛` 当前只读统计为 222 个视频文件、216 个 NFO，缺少 NFO 的 6 个视频正是：

```text
GOMK-051.avi.strm
MXGS-1183.mp4.strm
DV-803.avi.strm
MXGS-1121.mp4.strm
DV-859.mp4.strm
MXGS-224.mp4.strm
```

其中 `DV-803`、`DV-859`、`MXGS-224` 属于媒体库侧的规范化、标题解析或刮削失败链路；`GOMK-051` 是 `GOMK-51` 的补零写法；两个 `MXGS` 文件则是磁力 `dn` 别名问题。此前日志中已经能看到同一批记录的解析或写入交错，不能仅凭“日志有解析成功”判断每个文件都写入了完整 NFO。

## 修复内容

### 视频整理器

- 在预加载爬虫结果时构建磁力别名索引，不在扫描每个文件时读取 JSON。
- 只解析磁力 URI 的 `dn` 参数，不使用 info hash、tracker 或任意网页文本作为番号证据。
- 支持连字符、下划线、无分隔符、域名前缀和补零形式，例如 `MXGS1121`、`hhd800.com@MXGS-1183`、`kfa55.com@MXGS1358`。
- 磁力别名还必须与规范番号保持同一前缀，并且是规范数字的连续扩展；跨前缀或无关数字不会被当成别名。
- 别名必须唯一映射到一个规范 `CodeEntry`；同一个别名对应多个规范番号时，严格模式继续判定为未命中。
- 严格模式命中别名后，`Candidate.FilmCode` 写入规范番号，最终整理名使用规范番号，不会生成 `MXGS-1121` 这样的新番号目录。
- 日志会输出“磁力别名命中爬虫名单：别名 -> 规范番号”，便于确认实际命中来源。
- “名称抢救”流程复用同一套别名规则，避免优化软件时把文件再次放入未命中目录。

### 媒体库刮削器

此前已完成的相关修复保留并继续生效：

- 标题支持 `DV-803 【...】` 这类番号后直接跟空格和标题的格式。
- URL 支持 `DV-803_2024-10-11` 日期后缀。
- 选择一个爬虫产物后只使用该产物，不再因产物读取失败而扫描全部本地视频。
- 磁力别名和补零番号先回溯到规范记录，再用规范记录请求元数据；本地文件的显示名仍用于 sidecar 文件名。
- 选定快照包含演员信息时，刮削源会过滤明显不属于目标演员的记录，避免 `MXGS-1358`、`MXGS-1183` 一类跨演员污染。

## 严格匹配边界

下列情况仍然会进入“未命中”，这是预期的安全行为：

1. 文件名没有番号候选，且历史改名证据也无法提供番号。
2. 候选番号不在爬虫规范名单，也没有对应磁力 `dn` 证据。
3. 一个磁力 `dn` 同时映射到多个规范番号。
4. 一个文件路径同时包含多个互相冲突的有效番号。

这些情况不能通过“更宽泛的模糊匹配”解决，否则会违背严格名单的安全目标。程序会把文件保留在未命中目录并在报告中说明原因。

## 验证结果

已通过整理器单元测试和严格模式集成测试，覆盖：

- `MXGS1121` -> `MXGS-112`。
- `hhd800.com@MXGS-1183` -> `MXGS-118`。
- `kfa55.com@MXGS1358` -> `MXGS-135`。
- `GOMK-051` 等数字补零变体。
- 多规范番号争用同一个别名时拒绝匹配。
- 严格模式命中后输出规范名 `MXGS-112.mp4`，不再进入未命中列表。

已执行：

```text
go test ./internal/organizer
```

下一步在实际应用中需要重新加载最新爬虫快照，再重新运行一次视频整理或“名称抢救”。旧的 `未识别番号视频.txt` 是历史运行报告，不会自动回写；重新运行后应以新报告为准。
