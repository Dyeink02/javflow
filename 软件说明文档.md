# JavFlow 软件说明文档

## 1. 软件定位

**JavFlow** 是 Windows 桌面端的一体化 JAV 媒体库自动化工具，将「抓片、整理、订阅、刮削」串成一条流水线。核心能力包括：

1. **JAV 爬虫**：抓取影片元数据与磁力链接
2. **视频整理**：按番号识别、去广告、去重、重命名本地视频
3. **AV 订阅**：追踪女优 / 片商 / 系列更新并自动抓取
4. **媒体库刮削**：生成 NFO、海报、背景图，兼容 Emby / Jellyfin

> 100% 本地运行，不收集、不上传任何文件信息。

---

## 2. 运行环境

- Windows 10 / 11（64-bit）
- 建议使用可用代理以提升稳定性
- 输出目录需预留充足磁盘空间

---

## 3. 版本信息

**当前版本：v0.4.0**

### v0.4.0 近期更新摘要

- **品牌升级**：软件正式更名为 **JavFlow**，并采用全新流动风格图标。
- **发布日期过滤**：爬虫支持按最早发布日期过滤，避免刮削过旧影片。
- **反封锁增强**：TLS 握手失败时自动 fallback 到镜像地址；代理状态实时监控。
- **媒体库刮削修复**：修复 backdrop 图片未实际下载却显示成功的状态问题。
- **过滤结果可见性**：被过滤的番号现在会写入 `filtered-film-codes.json`，下游整理与订阅模块可完整读取。
- **UI 优化**：
  - 订阅、历史快照、榜单缓存均按更新时间倒序排列；
  - 爬虫完成后自动刷新整理、刮削、订阅面板；
  - 订阅面板精简按钮、URL 可点击、布局改为响应式网格。
- **隐藏文件默认显示**：媒体库刮削中隐藏路径文件默认可见，减少困惑。
- **演员头像缓存**：统一缓存到库根 `.actors` 目录，全库复用。

---

## 4. 功能区说明

### 4.1 JAV 爬虫

可配置：

- 抓取地址与反封锁镜像地址
- 抓取数量 / 总页数 / 每页条数
- 并行量、请求延迟、超时
- 代理地址
- Cloudflare 兼容开关
- 发布日期过滤、女优数量过滤、指定番号阈值过滤

输出：

- `filmData.json`
- `magnet-links.txt`
- `filtered-film-codes.json`（被过滤的番号详情）
- 运行日志与质量摘要

### 4.2 视频整理

用于本地视频的批量识别、重命名、归类和清理。建议先完成爬虫，再执行整理。

主要能力：

- 按番号自动识别文件名
- 自动保留最高画质 / 最大文件，低画质移入 `_to_delete`
- 同名番号后缀（如 `-A`、`-B`）保持原顺序
- 元数据文件（NFO、poster、backdrop、landscape）随视频一起重命名

### 4.3 AV 订阅

用于对已订阅女优、片商或系列做更新检测与更新抓取。

当前策略：

1. 扫描页面番号并与基线对比
2. 得到待更新番号后启动更新抓取
3. 抓取后仅保留待更新目标结果

### 4.4 媒体库刮削

扫描视频目录，自动下载封面、背景图、演员头像，并生成 Emby / Jellyfin 兼容的 NFO 文件。

演员头像统一缓存在库根 `.actors` 目录，全库复用。

---

## 5. 标准使用流程

1. 在 **JAV 爬虫** 配置参数并开始抓取
2. 检查运行日志与结果文件
3. 需要整理时进入 **视频整理**
4. 需要追更时进入 **AV 订阅**
5. 需要生成媒体库时进入 **媒体库刮削**

---

## 6. 常见问题

### 6.1 代理不通

先确认代理地址和端口可用，再看软件中的代理状态提示。

### 6.2 命中 Cloudflare / 年龄检测

开启 Cloudflare 兼容并确认代理可用。如果依然失败，优先检查网络环境，再复盘日志。

### 6.3 订阅检测到更新但输出为空

通常是「待更新番号未抓到」或「回收过滤后为空」。请查看订阅日志中的「待更新番号」「回收结果」「未抓到番号」条目。

### 6.4 演员头像在 Emby 中不显示

Emby 对本地 `.actors` 目录的识别不如 Jellyfin 稳定。建议：

- 关闭 Emby 的在线 People 下载器；
- 启用本地图片提取器并刷新元数据；
- 或配置 Emby API 密钥，让 JavFlow 在刮削后自动触发库刷新。

---

## 7. 输出与日志建议

1. 每次任务尽量使用独立输出目录
2. 保留 `run-时间戳/logs` 目录便于复盘
3. 反馈问题时附上完整日志目录路径

---

## 8. 未来计划

### 8.1 视频整理：智能广告片替换

在视频整理阶段自动识别「开头广告影片」，将其从目标目录移除，并自动重新获取对应番号的有效磁力链接，方便用户重新下载无广告版本。

### 8.2 JAV 爬虫：115 开放平台全流程打通

为 JAV 爬虫接入 **115 开放平台 API**，实现：

> 用户发起爬取请求 → 自动 115 离线下载 → 自动视频整理 → 生成 STRM → 媒体库刮削 → 自动添加订阅

一条龙闭环，无需手动切换多个面板。

### 8.3 JAV 爬虫：演员榜单快捷操作

在演员榜单中增加：

- 演员名称一键打开对应网址
- 一键跳转/启动该演员的爬虫任务

进一步减少页面切换和复制粘贴操作。

---

## 9. 相关文档

- 项目总览：`README.md`
- 开发维护：`开发文档.md`
- 使用说明：本文档
